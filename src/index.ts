import {
    createStore,
    GetState,
    StoreOptions,
    SetState,
} from "./lib/createStore";
import { useStore } from "./lib/useStore";

export {
    createStore,
    useStore,
    type SetState,
    type GetState,
    type StoreOptions,
};
